# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Accounting Together (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Accounting Together (iOS)
  pod 'Charts'
  pod 'RealmSwift', '~>10'

end

target 'Accounting Together (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Accounting Together (macOS)

end
